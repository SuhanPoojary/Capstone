<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Capstone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Capstone" parent="Base.Theme.Capstone" />

    <!-- Splash theme (launcher) -->
    <style name="Theme.Capstone.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/safeready_navy</item>
        <item name="android:statusBarColor">@color/safeready_navy</item>
        <item name="android:navigationBarColor">@color/safeready_navy</item>
    </style>
</resources>